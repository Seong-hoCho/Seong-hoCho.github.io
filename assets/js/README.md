# assets/js

Placeholder for future extraction of `daily-dashboard.html`'s inline `<script>` block
(~1800 lines).

Not done yet, for the same reason as `assets/css/README.md` — deferred to a follow-up
session so it can be tested live rather than edited blind.
